// Initialize AOS with proper settings
document.addEventListener('DOMContentLoaded', function() {
    AOS.init({
        duration: 1000,
        once: true,
        offset: 100,
        disable: 'mobile' // Optional: disable on mobile devices
    });
});

// Rest of your existing JavaScript code
// ... existing code ... 